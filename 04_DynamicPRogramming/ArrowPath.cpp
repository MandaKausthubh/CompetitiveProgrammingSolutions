#include<bits/stdc++.h>
#include <cstdint>
#include <vector>
using namespace std;

typedef struct {
    int x, y;
    vector<pair<int, int>> V;
} Node;

int32_t main(void) {
    int T; cin >> T;
    while(T--) {
        
    }
    return 0;
}
