#include <bits/stdc++.h>
#include <vector>
using namespace :: std;

int main(void) {
    int T; cin >> T;
    while(T--) {
        int n; cin >> n;
        vector<char> V(n);
        for(char &x: V) cin >> x;

        vector<int> Index;

        char maxi = V[n-1];

        for(int i = n-1; i > -1; i--) {
        }
    }
    return 0;
}
